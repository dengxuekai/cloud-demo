spring:
  application:
    name: gateway-zuul
  cloud:
    config:
      label: master
      profile: A
      discovery:
        enabled: true
        service-id: config-server
  zipkin:
    base-url: http://localhost:9411